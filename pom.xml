<project>
    <modelVersion>4.0.0</modelVersion>
    <groupId>ru.shishkov</groupId>
    <artifactId>ignite-memory-overcommit-test</artifactId>
    <version>0.1</version>

    <properties>
        <ignite-version>2.7.6</ignite-version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.apache.ignite</groupId>
            <artifactId>ignite-core</artifactId>
            <version>${ignite-version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
